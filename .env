# .env
# ข้อมูลการเชื่อมต่อฐานข้อมูล (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=your_database_name

# คีย์ลับสำหรับ JWT (JSON Web Token)
# !!! ควรเปลี่ยนคีย์นี้เมื่อใช้งานจริงใน Production !!!
JWT_SECRET=your_super_secret_key_12345# .env

